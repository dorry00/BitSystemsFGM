import { createTheme } from '@mui/material/styles';
const Color = {
    secondary:"#f50057",
    primary: "#2196f3",
    
    
    }

const theme = createTheme({
  pallete: {
    primary:{
        main: Color.teal,
    }
  },
});

export default theme; 

